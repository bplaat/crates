[package]
name = "argparse"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
argparse_derive = { path = "../argparse_derive", optional = true }

[features]
default = ["derive"]
derive = ["dep:argparse_derive"]
