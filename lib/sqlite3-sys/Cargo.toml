[package]
name = "sqlite3-sys"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true

[lints]
workspace = true

[build-dependencies]
cc = { version = "1.2.3", optional = true }

[features]
bundled = ["dep:cc"]
