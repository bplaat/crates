[package]
name = "sqlite"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
serde = "1.0"
uuid = { version = "1.11", optional = true }

[features]
uuid = ["dep:uuid"]
