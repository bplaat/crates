[package]
name = "native-tls"
version = "0.2.18+patch"
edition.workspace = true
license.workspace = true
authors.workspace = true

[features]
vendored = ["dep:openssl-src"]

[build-dependencies]
openssl-src = { version = "300.6.0", optional = true }

[lints]
workspace = true
