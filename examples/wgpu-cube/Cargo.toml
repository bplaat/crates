[package]
name = "example-wgpu-cube"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
publish = false

[dependencies]
bcanvas = { path = "../../lib/bcanvas" }
bwindow = { path = "../../lib/bwindow", default-features = false }
wgpu = { path = "../../lib/wgpu" }
image = { path = "../../lib/image", default-features = false, features = [
    "jpeg",
] }

[build-dependencies]
wgpu-shader-build = { path = "../../lib/wgpu-shader-build" }

[lints]
workspace = true
