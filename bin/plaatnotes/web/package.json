{
    "name": "plaatnotes",
    "private": true,
    "version": "0.1.3",
    "type": "module",
    "scripts": {
        "dev": "vite --mode debug",
        "build-debug": "tsc -p tsconfig.app.json --noEmit && vite build --mode debug --minify=false",
        "build-release": "tsc -p tsconfig.app.json --noEmit && vite build --mode release",
        "test": "NODE_OPTIONS=--no-deprecation playwright test"
    },
    "dependencies": {
        "plaatui": "file:../../../npm-lib/plaatui",
        "@preact/signals": "^2.3.1",
        "@types/dompurify": "^3.0.5",
        "@types/turndown": "^5.0.6",
        "dompurify": "^3.3.3",
        "marked": "^17.0.3",
        "preact": "^10.26.9",
        "turndown": "^7.2.2",
        "wouter-preact": "^3.9.0"
    },
    "devDependencies": {
        "@playwright/test": "1.60.0",
        "@preact/preset-vite": "^2.10.1",
        "@types/node": "^26.1.0",
        "baseline-browser-mapping": "^2.10.0",
        "swagger-ui-dist": "^5.32.8",
        "typescript": "^5.9.3",
        "vite": "^8.0.0"
    }
}
