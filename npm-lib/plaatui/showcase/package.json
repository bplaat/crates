{
    "name": "plaatui-showcase",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build-debug": "tsc -p tsconfig.app.json --noEmit && vite build --base=/plaatui/ --minify=false",
        "build-release": "tsc -p tsconfig.app.json --noEmit && vite build --base=/plaatui/"
    },
    "dependencies": {
        "plaatui": "file:..",
        "preact": "^10.26.9"
    },
    "devDependencies": {
        "@preact/preset-vite": "^2.10.1",
        "@types/node": "^26.1.0",
        "typescript": "^5.9.3",
        "vite": "^8.0.0"
    }
}
